export default function handler(req, res) {
  // Placeholder list. Replace with DB query in production.
  res.status(200).json({ tickets: ['Example: Google auth fails', 'Example: Chat times out'] });
}
